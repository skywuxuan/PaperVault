name: Desktop Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build-windows:
    runs-on: windows-latest

    steps:
      - name: Check out source
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: requirements-desktop.txt

      - name: Install desktop dependencies
        shell: pwsh
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements-desktop.txt

      - name: Build Windows desktop bundle
        shell: pwsh
        run: .\build-windows.ps1 -SkipInstall

      - name: Create release archive
        shell: pwsh
        run: |
          $version = "${{ github.ref_name }}".TrimStart("v")
          $archive = "dist\PaperVault-v$version-windows-x64.zip"
          Compress-Archive -Path "dist\PaperVault" -DestinationPath $archive

      - name: Upload Windows archive
        uses: actions/upload-artifact@v4
        with:
          name: windows-x64
          path: dist/PaperVault-*-windows-x64.zip

  build-macos-arm64:
    runs-on: macos-14

    steps:
      - name: Check out source
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: requirements-desktop.txt

      - name: Verify Apple Silicon runner
        shell: bash
        run: test "$(uname -m)" = "arm64"

      - name: Install desktop dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements-desktop.txt

      - name: Build Apple Silicon application
        run: bash build-macos.sh --skip-install

      - name: Create release archive
        shell: bash
        run: |
          version="${GITHUB_REF_NAME#v}"
          ditto -c -k --sequesterRsrc --keepParent dist/PaperVault.app "dist/PaperVault-v${version}-macos-arm64.zip"

      - name: Upload macOS archive
        uses: actions/upload-artifact@v4
        with:
          name: macos-arm64
          path: dist/PaperVault-*-macos-arm64.zip

  publish-release:
    runs-on: ubuntu-latest
    needs: [build-windows, build-macos-arm64]

    steps:
      - name: Check out source
        uses: actions/checkout@v4

      - name: Download release archives
        uses: actions/download-artifact@v4
        with:
          pattern: "*-*"
          path: dist
          merge-multiple: true

      - name: Publish GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: PaperVault ${{ github.ref_name }}
          body_path: CHANGELOG.md
          files: dist/PaperVault-*.zip
          generate_release_notes: true
